CREATE TABLE "public"."machine_user" ("id" uuid NOT NULL DEFAULT gen_random_uuid(), "machine_id" text NOT NULL, "user_id" uuid NOT NULL, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL DEFAULT now(), PRIMARY KEY ("id") , FOREIGN KEY ("user_id") REFERENCES "public"."user_profile"("id") ON UPDATE restrict ON DELETE restrict, UNIQUE ("machine_id"));
CREATE EXTENSION IF NOT EXISTS pgcrypto;
